#include <stdio.h>
#include <stdlib.h>

int main()
{
    int age;

    printf("ENTREZ VOTRE AGE : \n");
    scanf("%d", &age);

    if (age < 18)
    {
        printf("VOUS ETES MINOR.\n");
    }
    else if (age >= 18) && (age < 30)
    {
        printf("VOUS ETES MAJEUR.\n");
    }
    else if (age >= 30) && (age < 60)
    {
        printf("VOUS ETES ADULTE .\n");
    }
    else if (age >= 60) 

    {
        printf("VOUS ETES SENIOR\n");
    }

    return 0;
}